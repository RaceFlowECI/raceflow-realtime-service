server:
  port: 8083

spring:
  application:
    name: raceflow-realtime-service

spring:
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: 6379
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: 5672

